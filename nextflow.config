/*
docker.enabled = true
docker.runOptions = '-u $(id -u):$(id -g)'

process {
    container = 'chromapipe:latest'
    cpus = 2
    memory = '4 GB'
}
*/

docker.enabled = false

process {
    cpus = 2
    memory = '4 GB'
}
